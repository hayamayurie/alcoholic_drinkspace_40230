<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <title>お酒の記録</title>
  </head>
<body>
  <header>
    <div class ="title">
      <div class ="title-tag">alcohol record space</div>
      <div class='nav'>
    <ul class='lists-right'>
      <li><%= link_to  current_user.nickname, class: "user-nickname" %></li>
      <li><%= link_to 'ログアウト', destroy_user_session_path, data: {turbo_method: :delete}, class: "logout" %></li>
      <li><%= link_to  "Post", new_alcohol_path, class: "post" %></li>
   </ul>
  </div>
  </div>
    </header>
<div class ="new_btn">
<%= form_with(model: @alcohol, local: true) do |f| %>
<%= render "shared/error_messages", model: f.object  %>

    <div class="post_title_category">
  <%= f.label :category_id, 'お酒の種類'%></div>

    <div class="caregory_id">
  <% @categories.each do |category| %>
  <%= f.radio_button :category_id, category.id %>
    <%= f.label "alcohol_category_#{category.id}", category.category %>
  <% end %>
   </div>

   <div class= "post_title">銘柄</div>
   <%= f.text_area :alcohol_name, class:"alcohols-text", id:"alcohols-name",size: "50x2", maxlength:"40" %>

   <div class= "post_title">特徴</div>
   <%= f.text_area :memo, class:"items-text", id:"item-info", size: "50x10" ,maxlength:"1000" %>

  <div class="post_title">画像の投稿</div>
  <div class="image_title">
    <%= f.file_field :image, id:"item-image" %></div>

   <div>
  <%= f.submit "更新" ,class:"sell-btn" %></div>
<% end %>
</div>